spring:
  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/liveproject
    password: atumar4031
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      show-sql: 'true'
      ddl-auto: update
  server:
    port: '8080'